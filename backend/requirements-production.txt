# Production requirements - Minimal dependencies for API deployment
# Excludes heavy ML libraries (torch, xgboost, etc.) that aren't needed for API

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9

# Database
sqlalchemy>=2.0.45
psycopg[binary]>=3.1.0
pgvector>=0.3.0

# Authentication
bcrypt>=4.0.0
PyJWT>=2.8.0
python-jose[cryptography]==3.3.0

# HTTP Client
httpx>=0.27.0
requests>=2.32.0

# Data Processing (lightweight)
pandas>=2.2.0
numpy>=1.26.0

# AI/LLM (Claude + embeddings)
langchain-anthropic>=0.3.0
anthropic>=0.75.0
# Local embeddings for RAG (uses sentence-transformers)
sentence-transformers>=2.2.2,<3.0.0

# Configuration
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Google OAuth
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1

# Utilities
python-dotenv>=1.0.0
pytz==2023.3
email-validator>=2.1.0

# Optional: Basic technical analysis (lightweight)
ta>=0.11.0

# Data fetching
yfinance==0.2.32
pandas>=2.2.0,<3.0.0
numpy>=1.26.2,<2.0.0



# Note: Heavy ML dependencies (torch, xgboost, sentence-transformers, etc.) 
# are excluded for production deployment. They're only needed for:
# - Advanced ML models (not used in API)
# - Local development/analysis
# Use requirements.txt for full development environment
