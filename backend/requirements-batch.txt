# ML Training Batch Container — minimal dependencies
# torch installed separately: pip install torch --index-url https://download.pytorch.org/whl/cpu

# Core ML
numpy>=1.26,<2.0
pandas>=2.1
scikit-learn>=1.5,<2.0

# Data sources
yfinance>=0.2.31

# Data formats
pyarrow>=14.0

# Config
pyyaml>=6.0

# AWS S3 (artifact upload/download)
boto3>=1.34

# Database (Neon metadata writes — optional)
psycopg[binary]>=3.1
sqlalchemy>=2.0
