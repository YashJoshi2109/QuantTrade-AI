# FastAPI and server
fastapi>=0.115.0  # 0.104.1 had PYSEC-2024-38 (SSRF via docs redirect)
uvicorn[standard]>=0.32.0
pydantic>=2.7.4
pydantic-settings>=2.1.0
python-jose[cryptography]>=3.5.0  # JWT auth (3.5.0 fixes CVE-2024-33663/33664 algorithm confusion)
passlib[bcrypt]==1.7.4  # Password hashing
python-multipart>=0.0.28  # >=0.0.28 fixes DoS CVE-2024-24762

# Database
sqlalchemy>=2.0.45  # Updated for Python 3.14 compatibility
psycopg[binary]>=3.1.0  # Use psycopg3 for Python 3.14 compatibility
alembic==1.12.1
pgvector==0.2.4  # Vector extension for PostgreSQL

# Background jobs
celery==5.3.4
redis==5.0.1

# Data fetching
yfinance>=1.2.2
pandas>=2.2.0
numpy>=1.26.2
requests>=2.33.0  # <2.33.0 had CVE-2024-35195 (proxy credential leak) + SSRF fixes
beautifulsoup4==4.12.2
httpx==0.25.2
lxml>=5.3.1  # >=5.3.1 fixes CVE-2024-55549/CVE-2025-24928; For Finviz HTML parsing

# Finviz stock data
finviz

# Reddit content ingestion
asyncpraw>=7.7.0

# Alpaca Markets — free historical bars, US quotes (paper/live)
alpaca-py>=0.13.0

# Robinhood — US stock quotes fallback
robin_stocks>=3.0.0

# Public.com — real-time quotes, option chains, batch pricing (US stocks/ETFs/crypto/bonds)
publicdotcom-py>=0.1.10

# Technical analysis & feature engineering
ta==0.11.0
# pandas-ta>=0.3.14b0  # Optional: Requires Python < 3.14 (numba dependency)
# statsmodels==0.14.0  # Optional: May have Python 3.14 compatibility issues

# ML models
scikit-learn>=1.5.0,<2.0.0  # Updated for Python 3.14 compatibility
xgboost>=2.0.3,<3.0.0
lightgbm>=4.1.0,<5.0.0
shap>=0.43.0,<0.44.0  # Model explainability
torch>=2.0.0  # LSTM prediction model
pyyaml>=6.0  # ML training config
pyarrow>=14.0.0  # Parquet caching for ML data pipeline

# LLM + RAG
openai>=2.0.0,<3.0.0
langchain>=1.0.0
langchain-openai>=1.1.14  # <1.1.14 had credential exposure CVE
langchain-anthropic>=1.0.0
langchain-community>=0.4.0
sentence-transformers>=3.0.0
langchain-aws>=1.0.0
langgraph>=1.0.0
fastembed>=0.3.0
cohere>=5.0.0
pdfplumber>=0.11.0

# Payments / Billing
stripe>=11.0.0,<12.0.0

# Vector databases (choose one or both)
# Option A: pgvector (already included above)
# Option B: Dedicated vector DB
qdrant-client>=1.16.0,<2.0.0  # Qdrant client
# weaviate-client==3.25.3  # Weaviate client (uncomment if using)

# Object storage
boto3>=1.34.0  # AWS S3 compatible storage
minio==7.2.0  # Local S3-compatible storage option

# Utilities
python-dotenv>=1.2.2  # 1.0.0 had CVE-2026-28684
python-dateutil==2.8.2
pytz==2023.3
bcrypt>=4.0.0
feedparser>=6.0.0
PyJWT>=2.8.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
email-validator>=2.1.0
websockets
# PyPI package is "webauthn" (duo-labs/py_webauthn); "py-webauthn" is a different project (0.0.x only).
webauthn>=2.1.0
APScheduler>=3.10.0
elevenlabs>=2.42.0
defusedxml>=0.7.1
